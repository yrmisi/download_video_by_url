global:
  scrape_interval: 15s # Как часто собирать метрики

scrape_configs:
  - job_name: "fastapi-mediagrab"
    scrape_timeout: 10s
    metrics_path: "/metrics"
    static_configs:
      # Опрашиваем контейнер web напрямую через внутреннюю сеть Docker
      - targets: ["web:8000"]
